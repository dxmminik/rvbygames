<div class="row">
  <% @posts.each do |post| %>
    <div class="col-sm-2 mb-4">
      <div class="card text-center h-100 text-white bg-dark">
        <div class="card-body"><%= image_tag post.image.url, style: 'width:100%;' %></div>
        <div class="card-footer">
          <h5 class="card-title text-wrap font-weight-bold"><%= link_to post.title, post %></h5>
        </div>
        <div class="card-footer">
        <%= post.user.name if post.user %>
      </div>
        <% if post.user == current_user %>
          <div class="card-footer">
            <%= link_to 'Edytuj', edit_post_path(post) %>
            <%= link_to 'Usuń', post, method: :delete, data: { confirm: 'Czy jesteś pewien?' } %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>